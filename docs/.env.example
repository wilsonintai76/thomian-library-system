# ============================================================
# Thomian Library System — Environment Variables Template
# Copy this file to .env and fill in all required values.
# NEVER commit .env to version control.
# ============================================================

# ---- Django Core ----
# Generate with: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
DJANGO_SECRET_KEY=CHANGE_ME_generate_a_secure_key_here

# Set to False in production ALWAYS
DEBUG=False

# Comma-separated list. Add your domain and/or server IP.
ALLOWED_HOSTS=library.stthomas.edu,localhost,127.0.0.1

# ---- Database ----
DB_ENGINE=postgresql
DB_NAME=thomian_db
DB_USER=postgres
DB_PASSWORD=CHANGE_ME_strong_db_password
DB_HOST=db        # 'db' inside Docker Compose; 'localhost' on bare metal
DB_PORT=5432

# ---- CSRF / Security ----
# Must match your public HTTPS URL exactly
CSRF_TRUSTED_ORIGINS=https://library.stthomas.edu

# ---- CORS ----
# Comma-separated list of allowed frontend origins (no trailing slash)
CORS_ALLOWED_ORIGINS=https://library.stthomas.edu

# ---- Google Gemini AI (backend only — key is never sent to the browser) ----
# Required for AI book summaries and AI Auto-Map features
# Get a key at: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=CHANGE_ME_your_google_ai_studio_key

# ---- File Storage ----
# Set to False to use local /media folder (recommended for on-premise)
USE_S3=False

# ---- Optional: Cloudflare R2 / AWS S3 (only if USE_S3=True) ----
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_STORAGE_BUCKET_NAME=
# AWS_S3_ENDPOINT_URL=https://<accountid>.r2.cloudflarestorage.com
