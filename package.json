{
  "name": "thomian-lib-monorepo",
  "version": "3.7.21",
  "private": true,
  "scripts": {
    "dev:admin": "cd admin && npm run dev",
    "dev:kiosk": "cd kiosk && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "bump": "node bump.mjs",
    "bump:minor": "node bump.mjs minor",
    "bump:major": "node bump.mjs major",
    "deploy:admin": "npm run bump && cd admin && npm run build && npx wrangler pages deploy dist --project-name=thomian-admin",
    "deploy:kiosk": "npm run bump && cd kiosk && npm run build && npx wrangler pages deploy dist --project-name=thomian-kiosk",
    "deploy:backend": "npm run bump && cd backend && npx wrangler deploy",
    "deploy:all": "npm run bump && cd backend && npx wrangler deploy && cd ../admin && npm run build && npx wrangler pages deploy dist --project-name=thomian-admin && cd ../kiosk && npm run build && npx wrangler pages deploy dist --project-name=thomian-kiosk"
  }
}
